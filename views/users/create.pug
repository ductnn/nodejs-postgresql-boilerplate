extends ../layouts/common.pug

block content
    h1 Create users
    //- if errors
    //-     each error in errors
    //-         .alert.alert-danger= error
    form(action="/users/create", method="POST")
        .form-group
            label(for="name") Name: 
            input#name.form-control(name="name", type="text", value=values ? values.name : "")
        .form-group
            lable(for="phone") Phone: 
            input#phone.form-control(name="phone", type="number", value=values ? values.phone : "")
        .form-group
            lable(for="phone") Password: 
            input#phone.form-control(name="password", type="password", value=values ? values.phone : "")
        //- .form-group
        //-     label(for="avatar") Avatar:
        //-     input#avatar.form-control-file(name="avatar", type="file")
        button.btn.btn-info Create
